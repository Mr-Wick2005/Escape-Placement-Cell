{
  "name": "escape-placement-cell",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "yarn --cwd frontend build && node -e \"require('fs').cpSync('frontend/build', 'build', {recursive: true})\"",
    "start": "yarn --cwd frontend start",
    "test": "yarn --cwd frontend test"
  }
}
